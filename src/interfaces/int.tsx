export interface singleCarI {
  brand: string;
  model: string;
  body_type: string;
  tank_size: number;
  transmission: number;
  seats: number;
  price: number;
  thumbnail_url: string;
  loading?: boolean;
}
